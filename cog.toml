from_latest_tag = true
ignore_merge_commits = true
generate_mono_repository_global_tag = true
branch_whitelist = []

pre_bump_hooks = []

pre_package_bump_hooks = []

post_package_bump_hooks = []

post_bump_hooks = []

[commit_types]

[changelog]
path = "CHANGELOG.md"
authors = []
template = "remote"
remote = "github.com"
owner = "datron"
repository = "jilebi"

[bump_profiles]

[packages.dosa]
path = "crates/plugin-runtime"

[packages.jilebi_types]
path = "crates/jilebi_types"

[packages.store]
path = "crates/store"

[packages.jilebi]
path = "crates/jilebi"